# 请帮我写个中文的 Python 脚本，批注也是中文：
# 在脚本开始，询问源文件夹1（默认：d:\Works\Ins\），源文件夹2（默认：d:\Works\Inx\）。目标文件夹1（默认：d:\Works\Outs\），目标文件夹2（默认：d:\Works\Outx\）。
# 枚举源文件夹1里所有文件，依据源文件夹1里的每一个文件名（不包括后缀名），找到源文件夹2（包括子文件夹）里与之同名的文件（可以有多个，不包括后缀名）。
# 如果存在有匹配文件，则提示成功。源文件夹1里的文件，移动到目标文件夹1；源文件夹2里找到的多个文件，连同其父文件夹一同复制到目标文件夹2。如果存在有匹配文件，则跳过源文件夹1里的该文件。
# 所有文件完成后，列出源文件夹1里未成功匹配的文件名，写到剪贴板里。
# 完成后，再次跳回询问阶段，询问源文件夹1（默认：d:\Works\Ins\），源文件夹2（默认：d:\Works\Inx\）。目标文件夹1（默认：d:\Works\Outs\），目标文件夹2（默认：d:\Works\Outx\）。

# 导入模块
